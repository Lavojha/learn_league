"use client";

import Link from "next/link";
import { useEffect, useState } from "react";
type Material={id:string;title:string;description:string|null;visibility:string;status:string;availableFrom:string|null;availableUntil:string|null;downloadEnabled:boolean;accessDurationMinutes:number};
export default function GroupMaterialsPage({params}:{params:Promise<{groupId:string}>}){const[groupId,setGroupId]=useState("");const[materials,setMaterials]=useState<Material[]>([]);const[error,setError]=useState("");useEffect(()=>{params.then(({groupId})=>{setGroupId(groupId);fetch(`/api/materials?groupId=${groupId}`).then(async r=>{const d=await r.json();if(!r.ok)setError(d.error??"Unable to load materials");else setMaterials(d.materials??[]);});});},[params]);return <main className="mx-auto min-h-screen max-w-6xl px-6 py-10"><Link href={`/groups/${groupId}`} className="muted text-sm">← Group</Link><div className="mt-3 flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-sm text-violet-400">Group library</p><h1 className="text-3xl font-bold">Study materials</h1><p className="muted mt-1">PDFs shared with this group.</p></div><Link href={`/materials/upload?groupId=${groupId}`} className="rounded-xl bg-violet-500 px-4 py-3 text-center font-semibold">Upload PDF</Link></div>{error&&<p className="mt-6 text-rose-300">{error}</p>}<div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{materials.map(m=><article key={m.id} className="card p-5"><div className="flex items-start justify-between gap-3"><div><h2 className="font-semibold">{m.title}</h2><p className="muted mt-1 text-xs">{m.accessDurationMinutes} min access</p></div><span className="muted text-xs capitalize">{m.visibility}</span></div><p className="muted mt-3 line-clamp-2 text-sm">{m.description||"No description"}</p><div className="mt-4 flex flex-wrap gap-2 text-xs"><span className="rounded-full bg-white/5 px-2 py-1">{m.downloadEnabled?"Download on":"Download off"}</span><span className="rounded-full bg-white/5 px-2 py-1">{m.availableFrom&&new Date(m.availableFrom)>new Date()?"Scheduled":"Available"}</span></div><div className="mt-5 flex gap-2"><Link href={`/materials/${m.id}`} className="rounded-lg bg-white/10 px-3 py-2 text-sm">Open</Link><Link href={`/materials/${m.id}/settings`} className="rounded-lg bg-violet-500/15 px-3 py-2 text-sm text-violet-300">Settings</Link></div></article>)}{!materials.length&&!error&&<p className="muted">No materials available yet.</p>}</div></main>}
